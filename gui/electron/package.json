{
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build:electron": "tsc --project lib",
    "build:next": "next build && next export",
    "build": "npm run build:electron && npm run build:next",
    "start": "electron dist/index"
  },
  "dependencies": {
    "electron": "^11.3.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.2",
    "gui": "file:gui-1.0.1.tgz",
    "next": "^10.0.7",
    "next-transpile-modules": "^6.3.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "ts-standard": "^10.0.0",
    "typescript": "^4.2.2"
  }
}